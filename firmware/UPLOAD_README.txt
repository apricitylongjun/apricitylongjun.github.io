PT8 F407 GitHub Pages OTA upload directory

Upload the contents while preserving every firmware/pt8 path.
The releases/<version>-r<counter> tree is immutable release history.
Publish archive and current image.bin files before descriptor.bin files.
Publish the current firmware/pt8/manifest.json alias last.
This directory contains no private key.